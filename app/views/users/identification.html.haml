%body.mypage
  - if user_signed_in?
    = render "shared/loginHeader"
  - else
    = render "shared/header"
  - breadcrumb :mypage_identification
  = render "layouts/breadcrumbs"
  %main.container.clearfix
    .i-content.change-profile
      %h2.identification-content__head
        本人情報の登録
      %form.identification-form{action: "", method: "POST", novalidate: ""}
        .identification-form__list
          .form-textbox
            %p.form-textbox-top
              お客さまの本人情報をご登録ください。
              %br
              登録されたお名前・生年月日を変更する場合、本人確認書類の提出が必要になります。
            %p.form-textbox-bottom
              %a.form-textbox-bottom-text{href: "", target: "_blank"}
                本人確認書類のアップロードについて
          .form__group
            %label.lavel-text お名前
            %p #{@user.family_name_kanji} #{@user.first_name_kanji}
          .form__group
            %label.lavel-text お名前カナ
            %p #{@user.family_name_kana} #{@user.first_name_kana}
          .form__group
            %label.lavel-text{for: "birthday"} 生年月日
            %p #{@user.year}/#{@user.month}/#{@user.day}
          .form__group
            %label{for: "zip_code"} 郵便番号
            %span.form__group-arbitrary 任意
            %input.input-def{name: "zip_code", placeholder: "例）1234567", type: "text", value: ""}/
          .form__group
            %label{for: "prefecture"} 都道府県
            %span.form__group-arbitrary 任意
            %br
            %select.select-def{name: "prefecture"}
              = @prefecture.each do |item|
                %option{value: item[:prefecture]} #{item[:prefecture]}
          .form__group
            %label{for: "city"} 市区町村
            %span.form__group-arbitrary 任意
            %input.input-def{name: "city", placeholder: "例) 横浜市緑区", type: "text", value: ""}/
          .form__group
            %label{for: "address1_label"} 番地
            %span.form__group-arbitrary 任意
            %input.input-def{name: "address1", placeholder: "例) 青山1−1−1", type: "text", value: ""}/
          .form__group
            %label{for: "address2_label"} 建物名
            %span.form__group-arbitrary 任意
            %input.input-def{name: "address2", placeholder: "例) 柳ビル103", type: "text", value: ""}/
          %button.identification-btn-submit{type: "submit"} 登録する
          .form-group-textbox
            %p
              %a.form-group-text{href: "", target: "_blank"}
                本人情報の登録について
    .mypage-l-side
      = render 'shared/sidebar'
  = render "shared/footer"